using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class WindowOptionExample : MonoBehaviour
{

   


    //[SerializeField] private List<Toggle> _toggles = new List<Toggle>() { } ;
    // Start is called before the first frame update
    void Start()
    {
      

        /*foreach (Toggle toggles in _toggles)
        {
            _toggles[toggles].onValueChanged.AddListener(OnToggleValueChangedHandle);

        }*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }



  

}
